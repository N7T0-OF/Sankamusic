# ── Gradle ──────────────────────────────────────────────────────────────
.gradle/
build/
**/build/

# ── Configuration locale (machine) ─────────────────────────────────────
local.properties

# ── IDE ────────────────────────────────────────────────────────────────
.idea/
*.iml
*.ipr
*.iws
.vscode/
.kotlin/
captures/

# ── OS ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Outils (état local du client, pas le projet) ────────────────────────
.freebuff/

# ── 🔒 SECRETS ET KEYSTORES — NE JAMAIS COMMITER (docs/SECURITY.md) ────
*.jks
*.keystore
*.keystore.b64
*.p12
keystore*/
signing/
.tmp-signing/          # sortie de scripts/make-dev-keystore.sh (keystore + base64)

# Le jar du wrapper Gradle doit rester versionné (il n'est pas un secret)
!gradle/wrapper/gradle-wrapper.jar
