# Session de consolidation v1.9.0 — rapport consolidé (2026-08-26)

> Trace de la session de vérification/consolidation menée sur ce workspace
> (snapshot sans `.git`, `core/data` absent) en vue de la release SpaceKai
> v1.9.0 (code 73). Chaque entrée : ce qui a été trouvé, la preuve, le test.
> Le journal au fil de l'eau est `STATUS.md` ; ce document est la version
> consolidée prête à joindre à la PR de release.

## 1. Bugs réels trouvés et corrigés

| # | Bug | Preuve | Correctif | Test |
| --- | --- | --- | --- | --- |
| B1 | La propagation copiait le **mauvais changelog** : `ls … \| head -1` sélectionnait `1.txt` (le plus ancien, ère SimpMusic) — la v1.9.0 aurait publié les notes de la v1.0 | `apply-release-pipeline.sh` (~L150) | Sélection par code numérique de la source (73.txt), fallback plus élevé | Simulation : `SRC_CODE=73` → 73.txt sélectionné |
| B2 | `MIN_VERSION_CODE=71` périmé — v1.8.1 (code 72) a été publié (changelog 72.txt) ; 3 constantes ne suivaient pas | `verify-release.sh`, `apply-release-pipeline.sh`, `release-publish.sh` disaient « Highest = 71 » | Monté à **72** dans les 3 fichiers, commentaires corrigés | `grep MIN_VERSION_CODE` → 72 ×3 |
| B3 | `scripts/build_and_sign_apk.sh` référencé dans B_FILES — le fichier est à la racine → la propagation le **sautait silencieusement** | `apply-release-pipeline.sh` B_FILES | Chemin racine `build_and_sign_apk.sh` ; toutes les entrées A/B vérifiées existantes | `[ -f "$SOURCE/build_and_sign_apk.sh" ]` → OK |
| B4 | Header du changelog non surveillé — un header « SimpMusic v… » partait en release | aucun gate ne lisait la 1re ligne du changelog | Check « SpaceKai v<version> » (tolérant la forme `# ` du générateur) dans check-pre-tag §4b (bloquant) + pre-release-report (warn) | Header « SimpMusic v1.9.0 » → FAIL ; « # SpaceKai v1.9.0 » → PASS ; restore |

## 2. Trous comblés (contrôles ajoutés)

| # | Trou | Contrôle ajouté | Test |
| --- | --- | --- | --- |
| G1 | Les 3 listes de gates (release / pré-tag / nightly) pouvaient dériver sans détection — le motif « gate fantôme » | **`scripts/check-gate-parity.sh`** : extrait les invocations réelles des 3 chemins, FAIL si divergence ; intégré aux 3 chemins | Gate fantôme injecté → FAIL « missing/extra » ; restore → PASS (11 gates en parité) |
| G2 | `audit-features` exemptait SpaceKai.kt (liste IGNORED) → la suppression de l'appel de persistance au démarrage était invisible | Check de câblage de couche : `applyPersistedSpaceKaiFeatures` ≥ 1 call site hors déclaration | PASS (App.kt:175) ; FAIL simulé (App.kt masqué) ; restore PASS |
| G3 | La map FLAG de `generate-feature-audit` pouvait être polluée par des lignes non-flag (même pattern `^  PASS: <Nom>`) | L'extraction n'accepte que les vrais flags déclarés dans `SpaceKaiFeatures.kt` | 0 imposteur dans le rapport ; verdicts inchangés (1/10/9/0/2/1) |

## 3. Documents corrigés (dérives silencieuses)

| Doc | Dérive corrigée |
| --- | --- |
| `docs/ci-cd.md` | « the 9 audits » → **10 audits + gate parity** (le 10e `audit-provider-arch` n'avait jamais été documenté) ; « all 11 gates » → **12 code gates + APK** |
| `docs/PROVIDER-ARCHITECTURE.md` | « existing 11 gates » → 12 code gates |
| `STATUS.md` | 3 mentions périmées (nightly « 9 audits », pré-tag « 10 autres gates », liste des audits sans check-gate-parity) |
| `RELEASE.md` | Exemple de changelog `57.txt` (code n'existant plus) → placeholder `<VERSION_CODE>.txt` |
| `docs/ci-cd.md` | Référence morte `CollapsibleSection.kt` → vrais sites allowlistés (`FullWidthItems.kt:243/400`, `NowPlayingScreen.kt:1961`) |
| `docs/UPSTREAM.md` | **App.kt manquait de la liste des hotspots** (le point d'attache central : persistance :174, swipe, barres :491-506, analytics) ; table des conflits ne distinguait pas le package `spacekai/` (SpaceKai-owned, jamais présent chez upstream) ; hotspots barres + LogInViewModel ajoutés |

## 4. Claims du changelog 73 vérifiés

- **Les 2 lignes corrigées** (« no unified Minimalist/Translucent/Liquid-glass selector yet », « keeping the same tabs and order ») présentes telles quelles dans la note générée.
- **Ligne persistance** (« flags persisted / re-applied at startup ») **honnête** : `applyPersistedSpaceKaiFeatures` appelé à `App.kt:175`, `mergePersistedSpaceKaiFeatures` lu dans SpaceKaiSettingsSection:57, écriture `spacekai_<flag>` TRUE/FALSE + re-issue `configSpaceKai`.
- **Les 5 autres lignes** couvertes par les gates : nav (`audit-navigation`), paysage (`audit-landscape-player`), perf (`verify-perf-keys`), icônes (`verify-icons`), pipeline (`audit-upstream-hotspots`).
- **Verdicts du FEATURE AUDIT re-spot-checkés** : « sections de réglages fermées » = vraiment absent (les `AnimatedVisibility` de SettingScreen sont des visibilités conditionnelles, pas des sections repliables) ; « orientation » et « rail paysage » = exacts.

## 5. Note de release générée (flux CI exact : 73.txt → append-feature-audit-gaps.sh)

- **21 lignes** de gaps : 9 NOT IMPLEMENTED + 10 PARTIALLY + 2 NON VÉRIFIABLE (23 features − 2 entièrement faites).
- **6 « Toggle décoratif »** = exactement les 6 flags FAIL d'audit-features (1:1 vérifié).
- **5 lignes providers** (MusicProvider, Apple Music, Deezer, recherche multi-sources, musique locale) + **Android Auto** et **Widgets** présents.
- **0 contradiction** (`vs **` = 0), **0 « EspaceKai »**.

## 6. Plan de câblage P0 (le travail restant sur le live)

`docs/WIRING-P0.md` — pour chacun des 6 toggles factices : point d'insertion vérifié, câblage minimal, critère de passage.

| Flag | Point d'insertion (vérifié) |
| --- | --- |
| `minimalisticNavigation` | `App.kt:491-506` (sélection des barres) |
| `dynamicColor` | `Theme.kt:120-128` (wallpaperScheme / seedColor) |
| `landscapePlayer` | `NowPlayingScreen.kt` (branche paysage — le correctif artwork ~1036 est inconditionnel, c'est un bugfix) |
| `customPlayerInfo` | `NowPlayingScreen.kt:2455-2475` (rangées d'info) |
| `spotifySync` | `LogInViewModel.kt:28-37` (saveSpotifySpdc) + live `core/service/spotify` |
| `downloadWifiOnly` | **core/data (absent ici)** — seul câblage hors composeApp, à faire sur le live |

Règle posée : **jamais de référence morte pour faire passer le gate** — chaque câblage doit piloter une différence de comportement réelle.

## 7. État de la release v1.9.0

```
check-pre-tag.sh sur ce snapshot : 4 CRITICAL FAIL
  1. pas un dépôt git            → naturel (snapshot) / résolu sur le live
  2. pas de remote origin        → naturel (snapshot) / résolu sur le live
  3. gh absent                   → naturel (snapshot) / résolu sur le live
  4. audit-features FAIL         → LE vrai bloqueur : les 6 toggles factices
                                   (plan : docs/WIRING-P0.md)
```

Tout le reste du pipeline est vert : 11 gates en parité, 23/23 scripts syntaxe
OK, 0 contradiction de changelog, header valide, MIN_VERSION_CODE=72 cohérent.

## 8. Commandes pour le dépôt live (dans l'ordre)

```bash
# 1. Câbler les 6 toggles (docs/WIRING-P0.md), puis :
bash scripts/audit-features.sh            # doit finir : no decorative SpaceKai flags

# 2. Publication à UNE commande (vérifie tout, prévisualise, confirme) :
./scripts/publish.sh .                    # check-pre-tag + test-gates + dry-run + confirm
# équivalent manuel :
bash scripts/check-pre-tag.sh             # git/origin/gh + 12 gates + changelog vs audit
bash scripts/test-gates.sh                # 12 résultats attendus + 21 patterns critiques
                                          # + 3 simulations de câblage (retrait spotifySync,
                                          #   repli B downloadWifiOnly, câblage des 4 toggles)
./scripts/release.sh . --dry-run          # prévisualiser bump 73 → 74
./scripts/release.sh .                    # bump → commit → tag → push
# CI construit l'APK → draft → pre-release-report PASS → Publish

# 3. Release publique (une fois le draft CI construit) :
./scripts/publish-draft.sh [--yes]      # re-télécharge les assets → verify-release.sh
                                        # → bloc honnêteté présent ET frais (gap lines
                                        #   vs audit régénéré) → gh release publish

# 4. Post-publication :
verify-release.yml vérifie checksums + versionCode > 72
```

## 9. Fichiers nouveaux/ajoutés à propager (A_FILES)

`scripts/check-gate-parity.sh` · `scripts/check-pre-tag.sh` ·
`docs/PROVIDER-ARCHITECTURE.md` · `docs/WIRING-P0.md` · `docs/UPSTREAM.md` ·
`docs/ci-cd.md` · `docs/FEATURE-AUDIT.md` · `docs/release-notes-template.md` ·
`docs/SPACEKAI-ARCHITECTURE.md` · `RELEASE.md` · `build_and_sign_apk.sh` (B_FILES)
+ les workflows remplacés (B_FILES).
